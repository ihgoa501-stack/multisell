# 如何写好 Issue / Writing Good Issues

提 Issue 只有一个目标：**让看到的人不需要追问就能开始排查或实现。**

每多一轮追问 = 至少浪费一个维护者 10 分钟 + 你 2 分钟。下面用对比说明怎么写最省大家的时间。

---

## Bug 报告

### ❌ 差

> 创建商品报错了，帮我看看。

维护者内心：什么环境？哪一步报错？报的什么错？今天还是昨天？问一轮才能开始排查。

### ✅ 好

> ## Bug：创建商品时页面白屏
>
> **环境**
> - 版本：v0.2.1
> - OS：macOS 14.5
> - 浏览器：Chrome 125
> - API 地址：dev 环境
>
> **复现步骤**
> 1. 登录系统
> 2. 左侧菜单 → 商品管理 → 创建商品
> 3. 填完基本信息，点击「保存」
> 4. 页面变白，控制台报错见下方
>
> **预期行为：** 商品创建成功，跳转到商品列表页
>
> **实际行为：** 页面白屏，API 返回 500
>
> **报错日志**
> ```
> POST /api/v1/products 500 (Internal Server Error)
> Uncaught TypeError: Cannot read properties of undefined (reading 'name')
>     at ProductForm.tsx:142
> ```

**区别：** 看到这条 Issue，维护者可以直接定位到 `ProductForm.tsx:142` 看 `name` 字段哪里没初始化，不用问任何问题。

---

## 功能建议

### ❌ 差

> 加一个批量导入功能。

维护者内心：导入什么数据？什么格式？谁来用？解决了什么问题？这些不写清楚没法评估要不要做。

### ✅ 好

> ## Feature：商品批量导入（CSV）
>
> **背景**
> 运营每天要上架 50-100 个商品，目前逐个手动创建，一个人要花 2-3 小时。这部分工作重复性高、容易出错。
>
> **期望的解决方案**
> 在商品列表页增加「批量导入」按钮，支持上传 CSV 文件。CSV 模板包含：商品名、SKU、价格、库存、分类、图片 URL。
> 导入后显示成功/失败条数，失败的可下载错误报告。
>
> **替代方案**
> - 用 API 直接调，但运营不会用 Postman
> - 用 Excel 插件，但需要额外安装软件
>
> **适用场景**
> 运营团队（约 5 人），每天批量更新商品信息。预期每月导入 2000+ 商品。

**区别：** 维护者能直接评估工作量、确定优先级、判断是否值得做。

---

## 模板字段含义

| 字段 | 为什么要填 |
|------|-----------|
| **版本号** | 帮你确认这个 Bug 是不是已经在更新的版本里修掉了，避免白报 |
| **环境** | 不同 OS / 浏览器 / 数据库行为不同，不写这个可能无法复现 |
| **复现步骤** | 维护者按你的步骤走一遍才知道 Bug 在哪，一步都不能省 |
| **预期 vs 实际** | 防止维护者以为"这个行为是故意的"而关掉你的 Issue |
| **日志/截图** | 日志里往往直接写着报错位置，省去逐行排查的时间 |

---

## 提 Issue 前的自检清单

- [ ] 搜过现有 Issue，确认没有重复
- [ ] 是确定的 Bug 或功能需求，不是临时想法（临时想法 → **Discussions**）
- [ ] 版本号、环境、复现步骤都写全了
- [ ] 没有包含敏感业务数据（订单号、API Key、用户密码等）

---

## 维护者处理原则

- **好 Issue → 优先排期 → 修得快 → 结果有人看见**
- **信息不全的 Issue → 打 `needs-info` 标签 → 7 天无响应 → 自动关闭**

这是正向循环，不是惩罚。你花 3 分钟写清楚，维护者可能当天就修了。你花 10 秒随便写，拖一周自动关。

---

## 提交完怎么确认

- **Bug 模板** → 提交成功后会自动打上 `bug` + `status/triage` 标签，表示"排队等审"
- **Feature 模板** → 提交后自动打上 `enhancement` + `status/triage`
- **是否有人跟进** → 维护者会在 Issue 评论区回复，Triage 后会去掉 `status/triage` 并标上优先级
- **信息不足 → 等着** → 如果被打上 `status/needs-info`，说明缺信息，去评论区补充就行
- **长时间没动静 → 被关了** → 30 天无活动 → stale 标记 → 再 7 天无回应自动关闭，可以重新开

**一句话：按模板填完提交就行，剩下的维护者会接。**
