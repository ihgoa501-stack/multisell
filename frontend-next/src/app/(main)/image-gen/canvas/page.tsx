'use client';

import { Card } from 'antd';
import PageContainer from '@/components/ui/PageContainer';

export default function ImageGenCanvasPage() {
  return (
    <PageContainer title="AI 图片画布">
      <Card style={{ textAlign: 'center', padding: 'var(--space-3xl)' }}>
        <div style={{ fontSize: 16, color: 'var(--t3)' }}>画布功能开发中，敬请期待</div>
      </Card>
    </PageContainer>
  );
}
