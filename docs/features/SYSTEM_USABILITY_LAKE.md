# 功能任务：第一片湖——系统可用性恢复

> 添加时间：2026-07-11
> 提出人：Owner
> 优先级：P0
> 状态：in_progress

## 用户与场景

Owner 需要实际启动并使用凌镜，但当前系统无法稳定完成登录后的正常操作。

## 当前问题

- `actual`：Docker Desktop 当前未运行，Docker 入口不可用；本地 PostgreSQL 可用。
- `actual`：Go 后端可直接启动，`/api/health` 返回成功；Next 前端可启动。
- `actual`：现有 `e2e_test_user` 可通过 API 和 UI 登录。
- `actual`：登录后工具面板请求 `/api/v1/skus` 返回 403，并触发权限刷新递归，浏览器产生持续错误请求。
- `actual`：数据库实际有 107 张 public 表，但 `schema_migrations` 仅有 `version=1, dirty=true`；不得直接补跑迁移。
- `actual`：迁移目录 67、68、69、70 各有重复版本，现有迁移工具会在执行前失败。
- `actual`：审计日志缺少 `operation_log.user_id`，仪表盘缺少 `profit_summary`，数据库失配已经产生持续运行错误。
- `actual`：侧栏仍展示硬编码运行任务和统计，不属于真实经营状态。

## 期望现实结果

Owner 能在规定本地环境中连续两次完成启动、登录、进入核心页面、创建 mock 记录、保存、读取、修改、重启和持久化确认；失败时有明确错误且不产生请求风暴。

## 本次范围

- 对应海洋目标：恢复一个正常运转、可信、可控、可恢复的系统前置条件。
- 当前湖泊：系统可用性。
- 完成后连接到下一片湖的方式：Owner 能实际使用系统记录和处理一个自用决策。

当前先完成：

1. 阻止 403 权限刷新递归；
2. 选择不依赖未确认市场或商品的低风险 CRUD 页面；
3. 核对数据库结构风险，先备份再决定迁移修复；
4. 用真实浏览器完成两轮持久化验收；
5. 记录 mock、硬编码和真实数据边界。

## 明确不做

- 不直接应用 78 个未登记迁移；
- 不重置 Owner 凭证或扩大权限，除非 Owner 单独批准；
- 不连接真实平台或执行外部写入；
- 不把 mock 测试数据升级为商业证据；
- 不在当前湖增加相邻产品功能。

## 验收路径

1. 用户从 `http://127.0.0.1:3000/login` 进入。
2. 使用受控测试账户登录。
3. 进入选定的核心 CRUD 页面。
4. 创建一条名称含 `MOCK-SYSTEM-USABILITY` 的记录。
5. 保存后重新读取并修改。
6. 重启前后端后再次读取，确认数据持久化。
7. 全流程重复两次，控制台不得出现递归请求风暴。
8. 权限不足时只显示一次可行动错误，不循环刷新。
9. Owner 可按同一步骤操作，不需要阅读代码。

## 证据与完成等级

- `implemented`：阻塞缺陷和必要启动路径已修复。
- `automated_verified`：相关单元测试、文档检查和触及范围构建/测试通过。
- `manually_verified`：真实浏览器已完成一轮创建、读取、修改和后端重启持久化；完整两轮及数据库健康仍未通过。
- `external_observed`：不适用；当前是本地系统湖。
- `reconciled`：数据库迁移台账与实际结构一致后才能声明；当前未达到。
- `unknown`：生产环境、真实 Owner 凭证和外部平台仍未知。

## 风险与回滚

- 当前前端 403 修复和分类更新修复：低风险，可回退代码。
- RBAC、凭证和数据库迁移：高风险，必须先备份、说明影响并由 Owner 批准。
- 禁止在 `schema_migrations` 为 dirty 时直接执行全量迁移。

## 数据库隔离演练结果

- 已生成只读审计备份：`/tmp/multisell_readonly_audit_20260711_234033.dump`。
- 已成功恢复到隔离库 `multisell_audit_20260711_234033`，107 张表和抽查行数一致。
- 只在隔离库事务化演练 78–82，成功新增 8 张表且关键数据未变化；67–77 仍有缺表和缺字段。
- 结论：禁止对原库执行 `migrate force`、`migrate up` 或批量 down。需要先修正重复版本，设计幂等前向对账迁移，并在隔离副本完成整套验证。

## 等待 Owner 批准的高风险动作

批准后才可进入：修正迁移版本冲突、编制 67–77 对账迁移、在隔离副本完整演练、生成新备份，并在维护窗口对原 `multisell` 数据库执行已验证的单事务前向修复。任何失败立即停止，原库回滚使用迁移前完整备份。

## 停止条件

- 涉及凭证重置、权限扩大、数据库写迁移或数据删除时停止并请求 Owner 决定。
- 发现当前数据库无法安全恢复时，保留证据并改用隔离副本验证，不修改原库。
- 两轮浏览器验收未通过，不得把湖泊标记完成。

## 完成记录

- 实际完成：修复 403 权限刷新递归；修复分类部分更新破坏状态和创建时间；共享数字字段改用数值输入；完成历史方向文档状态对齐。
- 实际验证：文档链接检查 0 缺失；后端分类测试与前端定向测试通过；真实浏览器完成分类创建、修改和后端重启持久化验证一轮。
- 仍然未知：原数据库完整安全对账方案尚未落库；Owner 正式账号、第二轮完整验收和生产状态仍未知。
- Owner 下一步最需要做：明确批准或拒绝上述原数据库高风险修复。普通代码与隔离验证不需要 Owner 选择技术方案。
