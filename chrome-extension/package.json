{
  "name": "lingmirror-sourcing-agent",
  "version": "0.2.0",
  "type": "module",
  "license": "UNLICENSED",
  "private": true,
  "description": "LingMirror Owner-only 1688 private collection extension",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test tests/*.test.mjs",
    "watch": "tsc --watch",
    "clean": "rm -rf build",
    "rebuild": "npm run clean && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "linkedom": "^0.18.13",
    "typescript": "^5.5.0"
  }
}
