{
  "name": "lingmirror-sourcing-agent",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "private": true,
  "description": "LingMirror Owner-only AI sourcing extension — collect Ozon and supplier evidence",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf build",
    "rebuild": "npm run clean && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/chrome": "^0.0.270"
  }
}
