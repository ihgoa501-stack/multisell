DATABASE_URL=localhost:5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=multisell
REDIS_ADDR=localhost:6379
JWT_SECRET=dev-secret-change-in-production
SERVER_PORT=8080

# LLM Configuration
# In production (ENV=production), LLM_PROVIDER and LLM_API_KEY are required.
# Supported: openai, anthropic, qwen, deepseek, azure, stub (dev only)
LLM_PROVIDER=stub
LLM_API_KEY=
LLM_MODEL=
LLM_BASE_URL=

# Environment: development | production
ENV=development
